import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: Scaffold(
        appBar: AppBar(
          title: Text(
            "APP DEVELOPMENT",
            style: TextStyle(
              fontWeight: FontWeight.bold,
              color: Color(0xff3288cd),
            ),
          ),
          centerTitle: true,
        ),
        body: Container(
          color: Colors.blue,
          height: 500,
          width: 500,
          child: Center(
            child: Text("welcome to Development",
                style: TextStyle(
                    color: Colors.white,
                    fontWeight: FontWeight.bold,
                    fontSize: 25)),
          ),
        ),
        
      ),
      
    );
  }
}
