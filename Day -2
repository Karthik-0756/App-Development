import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: Scaffold(
          backgroundColor: Colors.blue,
          appBar: AppBar(
            backgroundColor: Colors.blue,
          ),
          body: Column(
            children: [
              Center(
                child: Text("Education",
                    style: TextStyle(
                        fontWeight: FontWeight.bold,
                        color: Colors.white,
                        fontSize: 35)),
              ),
              SizedBox(height: 20),
              Center(
                child: Icon(
                  Icons.school,
                  size: 150,
                  color: Colors.white,
                ),
              ),
              SizedBox(
                height: 20,
              ),
              Center(
                child: Text("Mobile App",
                    style: TextStyle(
                        color: Colors.white,
                        fontWeight: FontWeight.bold,
                        fontSize: 30)),
              ),
              SizedBox(
                height: 30,
              ),
              Center(
                child: Text(
                    "Lorem ipseum dolor sit amet,consectetur adipiding edit,sed do eisnud tempor",
                    style: TextStyle(
                      color: Colors.white,
                      fontSize: 15,
                    ),
                    textAlign: TextAlign.center),
              ),
              SizedBox(
                height: 30,
              ),
              Center(
                child: ElevatedButton(
                    onPressed: () {}, child: Text("GET STARTED")),
              ),
              SizedBox(
                height: 30,
              ),
              Center(
                child: Text("Lorem ipseum dolor sit amet,consectetur ",
                    style: TextStyle(
                      color: Colors.white,
                      fontSize: 15,
                    ),
                    textAlign: TextAlign.center),
              ),
            ],
          )),
    );
  }
}
